# =============================================================================
# .env.example – copy to .env and customize for local docker compose usage
#
#   cp .env.example .env
#   docker compose up mysql-84
# =============================================================================

# MySQL root password (used by all MySQL service variants)
MYSQL_ROOT_PASSWORD=test

# PostgreSQL superuser password (used by all PostgreSQL service variants)
POSTGRES_PASSWORD=test
